# Copied into the firecrawl/firecrawl checkout's root .env by scripts/setup-firecrawl.sh
# Only variables actually referenced by docker-compose.yaml take effect there -
# see https://github.com/firecrawl/firecrawl/blob/main/SELF_HOST.md

USE_DB_AUTHENTICATION=false
NUQ_BACKEND=pg
PORT=3002
HOST=0.0.0.0
